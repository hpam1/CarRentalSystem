/**
 * 
 *//*
package ssdi.carrentalsystem.test.Login;

import static org.junit.Assert.*;

import org.junit.Test;
import org.mockito.Mockito;

import ssdi.carrentalsystem.dao.LoginDAO;
import ssdi.carrentalsystem.io.LoginIO;
import ssdi.carrentalsystem.model.LoginModel;
import ssdi.carrentalsystem.utils.Constants;

*//**
 * @author haarthi
 *
 *//*

public class TestLoginModel {

	@Test
	public void testLoginModelForValid() throws Exception {
		LoginIO loginIO = new LoginIO();
		LoginDAO mockDAO = Mockito.mock(LoginDAO.class);
		Mockito.when(mockDAO.validateLoginDetails(loginIO)).thenReturn(loginIO);
		
		LoginModel loginModel = new LoginModel();
		assertEquals(loginIO, loginModel.validateUserDetails(loginIO, mockDAO));
	}
		
	@Test(expected=Exception.class)
	public void testLoginModelForInvalid() throws Exception {
		LoginIO loginIO = new LoginIO();
		LoginDAO mockDAO = Mockito.mock(LoginDAO.class);
		Mockito.when(mockDAO.validateLoginDetails(loginIO)).thenReturn(null);
		
		LoginModel loginModel = new LoginModel();
		loginModel.validateUserDetails(loginIO, mockDAO);
	}
}*/